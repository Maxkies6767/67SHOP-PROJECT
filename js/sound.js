/* ═══════════════════════════════════════
   67SHOP — Sound System
   (ปิดระบบเสียงทั้งหมดตามที่ผู้ใช้ต้องการ)
   ═══════════════════════════════════════ */

const SFX = (() => {
  function play() {
    // ไม่ทำอะไรเลย (ปิดเสียง)
  }
  return { play };
})();
